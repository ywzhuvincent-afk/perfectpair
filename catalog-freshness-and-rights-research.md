# 产品资料库、定时更新与版权合规

> 适用范围：PerfectPair 的文胸与丝袜产品资料、尺码表、价格/库存、来源记录和更新系统。本文是产品与工程的风险控制方案，不构成任何司法辖区的法律意见；在启用真实来源或面向特定国家运营前，应由当地知识产权律师复核。

## 决策摘要

“最全、最新”不等于“把所有品牌网页复制到自己的数据库”。可持续的产品库应做到：

1. **以直接授权、官方 API、官方 feed 为主**；
2. **只保存确实能服务搜索、对比和尺码匹配的客观产品事实**；
3. **每个事实都保存来源、抓取时间、产品版本、验证状态和允许的使用范围**；
4. **任何自动发现都只能进入候选队列，绝不自动上线**；
5. **版权、条款、robots、数据集权利和商标风险在抓取前判断，不在收到投诉后才处理**；
6. **用户仍看到丰富、快速、有来源的资料，而不是大段被搬运的品牌营销文案。**

美国版权局明确区分事实与表达：事实本身通常不受版权保护，但网页中的照片、文字、图形和原创编排可能受保护。^1 加拿大也保护作者的文字、照片、图表与网页表达，且权利通常在创作时即产生。^2 因此，“公开可见”并不等于“可复制、可大规模抓取或可再发布”。

## 产品资料库的完整数据模型

### 1. 品牌与系列

| 数据 | 作用 | 优先来源 | 是否可自动更新 |
| --- | --- | --- | --- |
| 品牌名、官网、国家/地区、尺码体系 | 搜索、尺码解释、品牌页 | 官方 API/feed、品牌自填 | 是，需来源记录 |
| 系列/型号、生命周期（在售/停产/待验证） | 避免把旧款当作新款 | 官方 SKU/feed | 是，需人工复核下架 |
| 官方产品链接 | 让用户回到原始来源完成购买/核查 | 官方页面 | 是 |
| 商标/别名 | 搜索纠错 | 品牌授权或人工维护 | 不从第三方目录批量复制 |

### 2. 文胸型号与变体

产品级保存：款式、钢圈类型、罩杯结构、支撑等级、构造（鸡心高度、侧翼、肩带位置、背扣排数、下围弹性、钢圈宽度、罩杯深度）、材质成分、用途、规范化尺码体系、版型提示、状态和版本。

变体级保存：颜色、色系、尺码标签、条码/SKU、是否在售、价格、货币、零售商、库存状态、采集时间。每条价格或库存是一次带时间的观察，不能覆盖历史。

### 3. 丝袜型号与变体

产品级保存：丹尼数、透明度、腰头构造、脚趾结构、保暖和压缩等级、材质、功能、适用场景、版型提示、尺码体系、官方链接、状态和版本。

变体级保存：颜色、尺码、SKU、零售渠道、价格、库存和采集时间。丝袜的尺码表必须单独保存为“图表版本 + 尺码行 + 官方来源链接 + 观察时间”，不能把不同品牌的 M、C、2 直接等同。

### 4. 每一个字段都要回答的五个问题

```text
这个字段是什么？来自谁？何时观察？允许怎样使用？现在还可靠吗？
```

项目已有的 `product_observations`、`product_versions`、`bra_size_mappings` 与丝袜专属表，已经覆盖前四个维度。本轮新增的 [数据库迁移](supabase/migrations/202609140006_catalog_freshness_and_rights_controls.sql) 补足了来源权限、抓取检查点、ETag/Last-Modified、幂等键和权利请求处理。

## 来源分级：先合法，再全面

| 等级 | 可接入来源 | 允许自动读取与保存 | 是否可自动进入候选队列 | 是否能自动上线 |
| --- | --- | --- | --- | --- |
| A | 品牌官方 API、书面授权 feed、品牌后台导出 | 在合同/授权字段范围内 | 可以 | 不可以；仍需字段质量和人工审核 |
| B | 联盟网络或零售商授权产品 feed | 仅许可范围内的 SKU、价格、库存、图片/文案 | 可以 | 不可以 |
| C | 品牌公开产品页 | 仅在条款、robots、访问频率与字段范围均获书面/内部批准后，优先取最少的事实和链接 | 可以，但默认 `needs_review` | 不可以 |
| D | 搜索结果、市场平台、社媒、其他比价站或竞品资料库 | 不做批量采集；仅作为人工发现线索 | 不可以 | 不可以 |
| E | 品牌图片、模特图、长描述、FAQ、编辑文章、品牌评价、受保护数据库导出 | 没有明确许可证时不复制、不存储、不展示 | 不可以 | 不可以 |

### 为什么 robots 不够

RFC 9309 规定了网站让爬虫表达访问偏好的 `robots.txt` 协议，但协议本身不是访问授权。^3 所以系统必须同时通过四个门：

```text
书面/API/Feed 权限 → 条款审核 → robots 路径与频率 → 字段/内容使用范围
```

任何一扇门失败，来源状态就是 `paused`；不发请求、不绕过登录、验证码、反爬、付费墙或速率限制。

### 欧盟数据库权利的额外注意

即使底层有些数据是事实，欧盟数据库指令仍可能保护数据库的选择/编排，或在重大投资情形下保护对数据库内容的提取和再利用。^4 因此对欧洲品牌、零售商和第三方目录，不能因为“产品名和价格是事实”就批量复制目录；应优先获得 API/feed/许可，或自行独立核验每个必要事实。

## 内容使用规则：既降低风险，也不伤害体验

### 默认可展示的内容

- 自行标准化后的产品事实：品牌名、型号名、尺码范围、材质成分、构造枚举、丹尼数、价格观察、库存状态；
- PerfectPair 自己写的中性说明、比较理由和结构化评分；
- 对每个事实的来源标签、时间和回到官方页面的链接；
- 用户依法提交并经审核的结构化体验信号，且与品牌内容分层。

### 默认不可复制的内容

- 品牌产品图、模特图、Logo 文件、视频、包装设计；
- 品牌的完整/长篇产品描述、宣传语、编辑文章、FAQ；
- 其他网站的用户评论、星级、尺码表截图、数据库导出；
- “先下载再决定能不能用”的网页快照或原始 HTML 存档。

若明确授权了图片或文案，则每份资产要保存许可证/合同参考、许可范围、期限、归属、替代文本和到期时间。到期后先下线资产，产品客观事实可在独立的合法来源支持下继续保留。

### 商标处理

使用品牌名是为了识别和比较真实商品，不应该制造赞助、合作或官方关系的暗示。页面必须使用中性文字，例如“品牌资料来源”和“前往官方页面”，不能把品牌 Logo 当作本站认证标识；收到商标或内容纠正请求时应有明确的处理队列。

### 不把 fair use / fair dealing 当作采集许可证

加拿大的 fair dealing 与美国的 fair use 都是具体事实下的例外，不是“可复制 10%”或“标注来源即可”的安全港。加拿大法第 29 条列出研究、私人学习、教育、讽刺或戏仿等目的；^5 美国法的 fair use 需要综合目的、作品性质、使用量和对市场影响等因素判断。^6 对以商业可用产品库为目的的持续自动抓取，默认策略应是**取得许可或只从自己独立收集的最低限度事实中重新表达**，而不是依赖例外。

## 固定时间更新与失效保护

### 已实现的基础计划（UTC）

| 任务 | 生产路径 | 计划 | 目的 | 所有 Vercel 计划可用 |
| --- | --- | --- | --- | --- |
| 每日目录、规格和尺码表刷新 | `GET /api/ingestion/run` | `04:10 UTC` | 新品牌/型号、规格、正式尺码表、停售状态 | 是 |
| 每日价格与库存刷新 | `GET /api/ingestion/refresh/pricing` | `04:25 UTC` | 价格、促销和可用性观察 | 是 |

Vercel 的 Cron 以 `GET` 调用路由，时区固定为 UTC。^7 现有接口已改为支持 `GET`，并用 `CRON_SECRET` 保护。计划任务只能创建候选记录，不能发布产品。

如使用 Vercel Pro/Enterprise 或外部可信调度器，可将价格/库存路线改为 `15 */6 * * *`（每 6 小时一次），保持目录/尺码表每日刷新。Vercel Hobby 不支持每天多次的单个 Cron，且在小时内触发并非精确到分钟；因此基础配置优先保证不部署失败。^8

### 每次运行的安全流程

```text
固定 Cron / 受保护手动触发
  → 读取已批准、未过期的 source_rights_review
  → 获取 robots/条款的已审核策略和速率上限
  → 只调用该来源允许的 API/feed/path
  → ETag / Last-Modified / cursor 条件请求
  → 原始候选（短期留存）+ 字段级来源 + 内容指纹
  → 质量与权限检查
  → needs_review / approved candidate
  → 人工审核、版本交易、发布
```

Vercel 不会自动重试失败 Cron，并可能出现重复投递或并发运行；官方建议同时使用锁和幂等设计。^9 因此生产持久层必须：

- 以 `source_id + lane + scheduled_time` 生成幂等键；
- 使用数据库 advisory lock 或 Redis 锁阻止同一来源/通道并发；
- 用 `source_refresh_checkpoints` 保存 cursor、ETag、Last-Modified、失败次数和下一次 due；
- 失败时指数退避、暂停阈值和人工告警，不无限重试；
- 失败不删除已发布产品；前台显示“上次确认时间”或“资料待复核”，而不是假装实时。

## 数据库与代码已新增的保护

| 层 | 已新增内容 | 实际阻止的风险 |
| --- | --- | --- |
| Source Adapter | `rights`：条款/robots 链接、审核日、到期日、允许字段、内容处理级别、自动访问/发布许可、速率、User-Agent、凭据参考 | 开发者把“公开页面”误当可用来源 |
| Ingestion Runner | 在访问前检查权限；过期、未允许自动访问、未允许发布或未定义字段则拒绝运行 | 未审核来源被定时任务访问 |
| Quality Gate | 拒绝未许可的图片、描述、营销文案、品牌评价等创意字段；拒绝来源未允许的标准化字段 | 原始内容从候选流程泄漏到网站 |
| `source_rights_reviews` | 版本化的条款/robots/许可证审核记录 | 口头或过期授权无法审计 |
| `source_refresh_checkpoints` | ETag、Last-Modified、游标、失败次数、下次更新时间 | 重复下载、无变化重抓、无法恢复 |
| `ingestion_runs` | refresh lane、触发来源、幂等键、权限审核 ID、请求/无变化计数 | 重复任务、无法追溯更新来源 |
| `catalog_rights_requests` | 版权、商标、条款、数据修正请求的处理状态 | 权利人请求被遗漏或只靠邮箱处理 |
| 前台产品页 | “Data & rights”说明 | 用户知道看到的是有来源的事实，不误以为本站拥有品牌创意资产 |

## 运营标准：什么才算“最新”

资料完整度不应以记录数量衡量，而以可验证性衡量。每个已发布 SKU 最低应有：

- 可访问的官方/授权来源 URL；
- 最后观察时间与产品版本；
- 至少一个尺寸或构造字段的来源；
- 价格/库存的观察时间；
- 适用于该来源的权利审核版本；
- 若超过阈值未刷新，则显示 `stale`，不继续宣称“最新”。

建议服务等级：目录和尺码表 7 天未确认即标记待复核；价格/库存 30 小时未确认即标记“上次观察”；下架/404、权利撤销、robots 变更或条款禁止自动访问即立即暂停来源并排队人工复核。

## 上线前必须完成的真实接入清单

1. 与目标品牌、零售商、联盟网络签订/保存 API、feed 或内容许可证明；把每一份权限录入 `source_rights_reviews`。
2. 为每个来源限定允许的 URL 路径、字段、频率、User-Agent 和使用范围；没有书面允许时默认不抓。
3. 通过服务端密钥将 runner 接到 Supabase：创建 run、锁、检查点、候选、人工审核和发布 transaction；不要让浏览器直连来源或持有来源密钥。
4. 建立审核后台：字段 diff、来源快照、版本比较、图片/长文案许可状态和“暂停来源”按钮。
5. 发布公开的数据政策、版权/商标联系渠道和更正/下架 SLA；对有效通知先限制争议内容，再保存证据和处理记录。
6. 对首批 20 个品牌逐一做 30 天试运行：每日变化率、抓取失败率、需人工复核率、版权/条款状态、用户点击官方链接和错误率。

## Sources

1. U.S. Copyright Office, “[What Does Copyright Protect?](https://www.copyright.gov/help/faq/faq-protect.html),” accessed September 2026.
2. Innovation, Science and Economic Development Canada, “[About copyright](https://ised-isde.canada.ca/site/ised/en/about-copyright),” accessed September 2026.
3. IETF, “[RFC 9309: Robots Exclusion Protocol](https://www.rfc-editor.org/rfc/rfc9309.html),” September 2022.
4. European Parliament and Council, “[Directive 96/9/EC on the legal protection of databases](https://eur-lex.europa.eu/eli/dir/1996/9/oj),” consolidated version accessed September 2026.
5. Justice Laws Website, Government of Canada, “[Copyright Act, section 29](https://laws-lois.justice.gc.ca/eng/acts/C-42/section-29.html),” current to June 21, 2026.
6. U.S. Copyright Office, “[Fair Use Index](https://copyright.gov/fair-use/),” accessed September 2026.
7. Vercel, “[Cron Jobs](https://vercel.com/docs/cron-jobs),” accessed September 2026.
8. Vercel, “[Usage & Pricing for Cron Jobs](https://vercel.com/docs/cron-jobs/usage-and-pricing),” accessed September 2026.
9. Vercel, “[Managing Cron Jobs](https://vercel.com/docs/cron-jobs/manage-cron-jobs),” accessed September 2026.
